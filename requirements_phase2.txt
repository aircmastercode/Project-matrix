# LendenClub Voice Assistant - Phase 2 Requirements (Text-only RAG)
# All dependencies are free and open-source

# ===== PHASE 1 DEPENDENCIES (Existing) =====
# Web scraping
requests>=2.31.0
beautifulsoup4>=4.12.3
lxml>=4.9.3

# Intent classification (Free alternatives to GPT-4o)
transformers>=4.35.0
torch>=2.0.0
accelerate>=0.24.0

# Traditional ML for fallback
scikit-learn>=1.3.0
numpy==1.25.2
pandas>=2.0.3

# Testing and validation
pytest>=7.4.0
pytest-cov>=4.1.0

# Utilities
python-dotenv>=1.0.0

# ===== PHASE 2 NEW DEPENDENCIES (RAG System) =====
# Vector database and embeddings
faiss-cpu>=1.7.4              # CPU-only FAISS for vector search
sentence-transformers>=2.2.2   # Free embedding models

# Text processing
nltk>=3.8.1                   # Natural language processing
spacy>=3.7.0                  # Advanced text processing (optional)
textstat>=0.7.3               # Text readability metrics

# Response generation and templates
jinja2>=3.1.2                 # Template engine for responses
regex>=2023.10.3              # Advanced regex for text extraction

# Performance and monitoring
psutil>=5.9.0                 # System resource monitoring
tqdm>=4.66.0                  # Progress bars

# Data visualization (for analysis)
matplotlib>=3.7.0
seaborn>=0.13.0

# File handling
pathlib>=1.0.1               # Path utilities (built-in Python 3.4+)
json5>=0.9.0                  # Enhanced JSON handling

# ===== OPTIONAL DEPENDENCIES =====
# For better performance (GPU acceleration)
# faiss-gpu>=1.7.4             # Uncomment if you have CUDA GPU
# torch[cuda]>=2.0.0           # Uncomment for GPU PyTorch

# For advanced text processing
# spacy[en_core_web_sm]         # English language model for spaCy

# ===== DEVELOPMENT DEPENDENCIES =====
# Code quality
black>=23.0.0                 # Code formatting
flake8>=6.0.0                 # Linting
mypy>=1.5.0                   # Type checking

# Documentation
sphinx>=7.1.0                 # Documentation generation
sphinx-rtd-theme>=1.3.0       # Read the Docs theme

# ===== NOTES =====
# 1. All models used are free and open-source
# 2. No API keys required for core functionality  
# 3. Total download size: ~2GB for all models
# 4. RAM requirement: 4GB minimum, 8GB recommended
# 5. GPU acceleration is optional but improves performance

# ===== INSTALLATION COMMANDS =====
# Standard installation:
# pip install -r requirements_phase2.txt

# With GPU support (if available):
# pip install -r requirements_phase2.txt faiss-gpu torch[cuda]

# With all optional dependencies:
# pip install -r requirements_phase2.txt spacy[en_core_web_sm]
